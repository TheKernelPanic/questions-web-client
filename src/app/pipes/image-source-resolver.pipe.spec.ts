import { ImageSourceResolverPipe } from './image-source-resolver.pipe';

describe('ImageSourceResolverPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSourceResolverPipe();
    expect(pipe).toBeTruthy();
  });
});
