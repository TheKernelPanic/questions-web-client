import { DateUtcToLocalRawFormatPipe } from './date-utc-to-local-raw-format.pipe';

describe('DateUtcToLocalRawFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateUtcToLocalRawFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
